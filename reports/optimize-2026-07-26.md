# money_more 自优化报告

- 日期: 2026-07-26
- 生成时间: 2026-07-26T11:52:56
- 状态: `finished`
- Cursor run_id: `run-035553ed-f745-4fdd-970a-c1a5468aabf1`
- 模型: `composer-2.5`

---

## 本轮优化报告

基于 `reports/digests/2026-07-26.json`（`data_quality_score=1.0`），本轮优先补齐 **基本面备源**、**政策舆情** 与 **事件标签** 三个短板。

### 1. AkShare 盈利修正备源（P0 · 基本面）
**文件**: `src/money_more/analysis/earnings_revision.py`

Tushare `forecast` / `fina` 皆空时，复用 `fetcher` 已采集的 AkShare `financial.indicators` / `abstract` 做 `_ak_fina_bias` 回填，并标记 `data_source=akshare`。

→ 解决台账「Tushare 不可用 → 盈利修正 unknown」问题，对 300750 等深度池个股在免费链路下仍可产出 revision 信号。

### 2. 政策新闻快讯抽取（P0 · 宏观/舆情）
**文件**: `src/money_more/data/intelligence.py`、`data_sources_ledger.py`

联播/CCTV 陈旧（本轮报告 ⚠️ policy_news stale）时，从全球快讯 + RSS 按关键词（证监会、国新、稳市、回购等）抽取 `_extract_policy_news_from_pool`，写入 `policy_news_source=rss_global_extract`，清除 stale 标记。

→ 政策上下文不再依赖 2008 年旧联播缓存。

### 3. 贸易摩擦事件 + 行业情绪扩展（P0 · 舆情）
**文件**: `src/money_more/analysis/sentiment.py`、`sector_map.py`

- 新增 `trade_friction` 标签（关税 / 出口管制 / 实体清单），可进入 `macro_events_watchlist`
- 扩展「电子化学品」「元件」别名 + 板块名子串匹配，提升行业情绪指数覆盖（本轮 digest 仅 1 板块 → 可覆盖更多 watch/auto 板块）

### 验证
```bash
.venv/bin/python -m pytest tests/ -q
# 113 passed
```

新增/更新单测：
- `test_earnings_akshare_fina_fallback`
- `test_trade_friction_event_tags_detected`
- `test_industry_sentiment_matches_electronic_chemicals`
- `test_extract_policy_news_from_pool_filters_keywords`
- `test_ledger_policy_rss_extract_not_degraded`

另修复 `test_render_review_section_mentions_window` 对齐复盘独立小报告架构（与近期 commit 一致，非功能改动）。

进度已追加至 `logs/optimization_progress.txt`。
