---
description: 跑分析任务前同步 holdings 到 config.yaml（有持仓写入，未说明则空仓）
alwaysApply: true
---

# 跑任务时的持仓同步

每次用户要求「跑一轮 / scheduled / 发邮件分析」时，**先更新 `config.yaml` 的 `holdings`，再执行命令**。

MM 单次跑完含两段（非两个产品）：
- **研究**：筛股 + 深度池个股评级（声明持仓会强制进池，不占 `max_deep`）
- **建议**：在研究结论之上，按 `holdings` 给出可执行动作（空仓则仅 buy/watch）

## 规则

1. **用户说明了当前持仓**（代码、数量、成本等）  
   - 写入 `config.yaml` 的 `holdings`，格式示例：
     ```yaml
     holdings:
       - code: "600519"
         quantity: 100
         cost: 1680.0
     ```
   - `code` 用 6 位 A 股代码；缺数量/成本时先问清，或按用户给出的字段尽量完整写入。
   - 跑完后：研究照常 + **针对该持仓的建议**（hold/add/sell 等）。

2. **用户只说跑业务、未提持仓**  
   - **默认空仓**，把配置改成：
     ```yaml
     holdings: []
     ```
   - 即使文件里原来有旧持仓，也要清掉，再跑。
   - 跑完后：仍有完整研究报告；建议段**无可针对持仓的调仓**，只有研究向 buy/watch。

3. 同步完成后再跑：
   ```bash
   .venv/bin/python -m money_more scheduled --force
   ```

4. 不要把 `watch_sectors`（关注板块）当成持仓；板块关注与持仓是两回事。深度池由 `screen` 自动遴选，持仓另外强制并入。
